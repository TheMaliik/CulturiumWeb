{# templates/museum/index.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des Musées</h1>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.localisation) }}
    {{ form_row(form.description) }}
    {{ form_row(form.image) }}
    {{ form_row(form.save) }}
    {{ form_end(form) }}
{% endblock %}
