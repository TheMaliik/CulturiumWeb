
{% extends 'base.html.twig' %}

{% block title %}Add Comment{% endblock %}

{% block body %}
    <h1>Add Comment</h1>
    {{ form_start(form) }}
        {{ form_row(form.description) }}
        {{ form_row(form.idPost) }} 
        <button type="submit" class="btn btn-primary">Save</button> 
    {{ form_end(form) }}
{% endblock %}