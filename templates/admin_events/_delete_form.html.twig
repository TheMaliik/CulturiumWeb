<form method="post" action="{{ path('app_admin_events_delete', {'ide': event.ide}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.ide) }}">
    <button class="btn">Delete</button>
</form>
