<form method="post" action="{{ path('livraison_delete', {'id': livraison.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette livraison ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livraison.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
