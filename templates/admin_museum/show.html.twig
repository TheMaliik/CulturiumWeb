{% extends 'base.html.twig' %}

{% block title %}Museum{% endblock %}

{% block body %}
    <h1>Museum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idm</th>
                <td>{{ museum.idm }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ museum.name }}</td>
            </tr>
            <!-- Remplacez cette ligne par la ligne ci-dessous -->
            <tr>
                <th>Image</th>
                <td><img src="{{ asset('uploads/' ~ museum.image) }}" alt="{{ museum.name }}"></td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ museum.description }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ museum.localisation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_museum_index') }}">back to list</a>

    <a href="{{ path('app_admin_museum_edit', {'idm': museum.idm}) }}">edit</a>

    {{ include('admin_museum/_delete_form.html.twig') }}
{% endblock %}
