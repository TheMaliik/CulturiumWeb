{% extends 'admin/main.html.twig' %}

{% block title %}Historique{% endblock %}

{% block contenpage %}
    <h1>Historique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ historique.id }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ historique.etat }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ historique.date ? historique.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_historique_index') }}">back to list</a>

    <a href="{{ path('app_historique_edit', {'id': historique.id}) }}">edit</a>

    {{ include('historique/_delete_form.html.twig') }}
{% endblock %}
